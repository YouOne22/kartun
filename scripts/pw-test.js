const { PrismaClient } = require("@prisma/client");const { PrismaPg } = require("@prisma/adapter-pg");const pg = require("pg");const dotenv = require("dotenv");const { hash, compare } = require("bcryptjs");dotenv.config();const cs = process.env.DATABASE_URL;const dbUrl = new URL(cs);const pw = decodeURIComponent(dbUrl.password);const pool = new pg.Pool({ connectionString: cs, password: pw });const adapter = new PrismaPg(pool);const prisma = new PrismaClient({ adapter });(async () => {  try {    const user = await prisma.user.findUnique({ where: { email: "ketua@tunasharapan.id" }, select: { passwordHash: true, isDefaultPassword: true } });    console.log("USER FOUND:", !!user);    if (user) {      console.log("HASH PREFIX:", user.passwordHash.slice(0, 30));      const match = await compare("kartunmaju", user.passwordHash);      console.log("PASSWORD MATCH:", match);      const freshHash = await hash("kartunmaju", 12);      const freshMatch = await compare("kartunmaju", freshHash);      console.log("FRESH HASH MATCH:", freshMatch);    }  } catch (err) {    console.error("ERROR:", err.message);  } finally {    await prisma.$disconnect();  }})();