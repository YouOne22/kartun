const { PrismaClient } = require('@prisma/client');const { PrismaPg } = require('@prisma/adapter-pg');const pg = require('pg');const dotenv = require('dotenv');dotenv.config();const connectionString = process.env.DATABASE_URL;const dbUrl = new URL(connectionString);const password = decodeURIComponent(dbUrl.password);const pool = new pg.Pool({ connectionString, password });const adapter = new PrismaPg(pool);const prisma = new PrismaClient({ adapter });async function main() {  const user = await prisma.user.findUnique({    where: { email: "ketua@tunasharapan.id" },    select: { id: true, memberId: true, fullName: true, email: true, passwordHash: true, isDefaultPassword: true, role: true },  });  if (!user) {    console.log('USER NOT FOUND');    const all = await prisma.user.findMany({ select: { email: true, role: true, passwordHash: true } });    console.log('All users:', all);    return;  }  console.log('USER:', user.email);  console.log('ROLE:', user.role);  console.log('isDefaultPassword:', user.isDefaultPassword);  console.log('passwordHash (first 30):', user.passwordHash.slice(0, 30));  console.log('passwordHash length:', user.passwordHash.length);  // Test compare with expected password  const bcrypt = require('bcryptjs');  const ok1 = await bcrypt.compare('kartunmaju', user.passwordHash);  console.log('compare(kartunmaju):', ok1);  const ok2 = await bcrypt.compare('password', user.passwordHash);  console.log('compare(password):', ok2);}main().catch(e => console.error(e)).finally(() => prisma.$disconnect());